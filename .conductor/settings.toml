#:schema https://conductor.build/schemas/settings.repo.schema.json

[scripts]
setup = "bash .conductor/setup.sh"
run_mode = "concurrent"

[scripts.run.dev]
command = "npm run dev -- -p $CONDUCTOR_PORT"
default = true
icon = "play"
